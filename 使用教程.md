# WorkBuddy2API 客户端接入使用教程

把本网关接到 Claude Code、Codex CLI、Cline、Continue、Aider、Cursor 等工具上。

> 本教程基于**实际运行中的服务**核实，不是照抄通用模板。地址与端口按默认部署（`127.0.0.1:7863`）书写；模型清单请以你自己 `/v1/models` 的实时返回为准。

---

## 0. 先搞清楚这个网关的接口形态

网关对客户端只暴露 **OpenAI 兼容**接口（`internal/server/handler.go:82-85` 注册了全部 4 条路由）：

| 端点 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/v1/chat/completions` | POST | Bearer（`api_key` 非空时） | 对话补全，**流式 / 非流式都支持** |
| `/v1/models` | GET | Bearer | 模型列表（动态拉取，缓存 1h） |
| `/status` | GET | Bearer | 账号池状态 |
| `/healthz` | GET | **无** | 探活，200 / 503 |

**默认部署的坐标（按需替换成你自己的）：**

| 项 | 值 |
|---|---|
| 基础地址（OpenAI 风格） | `http://127.0.0.1:7863/v1` |
| 鉴权头 | `Authorization: Bearer test_key` |
| 容器名 / 镜像 | `workbuddy2api` / `workbuddy2api-wb2api:latest` |
| 配置来源 | 上游仓库的 `config.json` |

> `api_key` 请以**你自己 `config.json` 里的实际值**为准（便携版/新装是首次运行时随机生成的 `wb2_...`）。要改就改这个文件再重启网关。
> 新装默认只监听 `127.0.0.1:7863`（只有本机能访问）；若显式改成 `0.0.0.0:7863` 或用了 docker compose 的端口映射，
> 同一局域网内的其他机器也能访问——务必同时换成强随机 key。

---

## 1. 三条铁律（先读，能省半小时）

### 铁律一：Claude Code 不能直连，必须用转换层

- 网关只有 OpenAI 格式的 `/v1/chat/completions`，**没有 Anthropic 的 `/v1/messages`**。
- Claude Code 说的是 Anthropic 格式。把 `ANTHROPIC_BASE_URL` 直接指向网关 → 请求打到不存在的 `/v1/messages` → 直接 404，页面里怎么写配置都没用。
- 解法：装一个本地翻译代理 **claude-code-router（CCR）**，把 Anthropic ↔ OpenAI 互译。链路是：

```
Claude Code ──Anthropic 格式──▶ CCR (127.0.0.1:3456) ──OpenAI 格式──▶ 网关 (127.0.0.1:7863) ──▶ CodeBuddy 上游
```

**Codex CLI、Cline/Roo、Continue、Aider、Cursor 等支持 OpenAI 格式的客户端则可以直连。**

### 铁律二：base URL 写法分两种，写错就 404

| 场景 | 该填什么 | 原因 |
|---|---|---|
| OpenAI 兼容客户端（绝大多数） | `http://127.0.0.1:7863/v1` | 客户端会自己追加 `/chat/completions` |
| claude-code-router 的 `api_base_url` | `http://127.0.0.1:7863/v1/chat/completions` | CCR 要求**完整**路径，不追加 |
| Claude Code 的 `ANTHROPIC_BASE_URL` | `http://127.0.0.1:3456` | 这是 **CCR 的地址**，不是网关地址 |

不要写成 `http://127.0.0.1:7863`（少了 `/v1`），也不要写成 `http://127.0.0.1:7863/v1/chat/completions` 再给会自己追加的客户端（会变成 `/v1/chat/completions/chat/completions`）。

### 铁律三：模型名用实时列表，别抄旧的

网关源码里写死了一张**静态回退表**（`handler.go:146-157`），里面的 `deepseek-v4-flash` 是**过时值**，在你当前实时列表里**并不存在**。真实叫 **`deepseek-v4.1-flash`**。

拿实时列表：

```powershell
curl.exe -s http://localhost:7863/v1/models -H "Authorization: Bearer test_key"
```

---

## 2. 开用之前：30 秒自检

依次跑这三条，全绿再配客户端，能省掉大半排错时间。

**① 网关活着吗？**

```powershell
curl.exe -s http://localhost:7863/healthz
# 期望：{"healthy":1,"total":1,"service":"workbuddy2api"}
```

**② 有哪些模型？**

```powershell
curl.exe -s http://localhost:7863/v1/models -H "Authorization: Bearer test_key"
```

**③ 端到端能通吗？（一次真实对话）**

```powershell
curl.exe -s http://localhost:7863/v1/chat/completions -H "Authorization: Bearer test_key" -H "Content-Type: application/json" -d "{\"model\":\"deepseek-v4.1-flash\",\"messages\":[{\"role\":\"user\",\"content\":\"只回复 OK\"}],\"stream\":false}"
```

返回带 `choices` 的 JSON = **端点、密钥、模型名三项全对**。之后任何问题都在客户端侧，不用再怀疑网关。

> 面板首页的「网关连通性测试」按钮等价于第 ③ 条。

---

## 3. 可用模型清单（以实时 `/v1/models` 为准）

| 模型 id | 上下文 | 最大输出 | 适合 |
|---|---|---|---|
| `deepseek-v4.1-flash` | 1,000,000 | 128,000 | **主力推荐**，长上下文 + 大输出 |
| `deepseek-v4-pro` | 1,000,000 | 50,000 | 复杂推理 |
| `glm-5.3` | 1,000,000 | 48,000 | 通用，均衡 |
| `glm-5.3-flash` | 1,000,000 | 32,000 | **快模型**（适合做 Claude Code 的 small/fast 档） |
| `glm-5.2` / `glm-5.1` | 1,000,000 / 200,000 | 48,000 | 上一代 |
| `glm-5v-turbo` | 200,000 | 64,000 | **多模态（视觉）** |
| `kimi-k3-1` | 1,000,000 | 32,000 | 长文 |
| `kimi-k2.8-preview` / `kimi-k2.7` / `kimi-k2.6` | 1M / 256K / 256K | 32,000 | — |
| `minimax-m3` | 512,000 | 128,000 | 大输出 |
| `hy4-preview` | 1,000,000 | 64,000 | 长文 |
| `hy3` / `hy3-x` | 192,000 | 64,000 | — |
| `auto` | 168,000 | 32,000 | 平台自动路由 |

> 「工具调用（function calling）」是**透传**的（`internal/upstream/payload.go` 只做 `tool_choice` 归一化，不动 `tools`），所以 Cline / CCR 这类依赖工具链的 agent 可以正常用。

---

## 4. 各客户端配置

### 4.1 Claude Code（需 CCR 中转）

**安装：**

```powershell
npm install -g @anthropic-ai/claude-code
npm install -g @musistudio/claude-code-router
```

**第一步：写 CCR 配置** —— `C:\Users\<你的用户名>\.claude-code-router\config.json`

```json
{
  "LOG": false,
  "APIKEY": "",
  "Providers": [
    {
      "name": "wb2api",
      "api_base_url": "http://127.0.0.1:7863/v1/chat/completions",
      "api_key": "test_key",
      "models": [
        "deepseek-v4.1-flash",
        "glm-5.3",
        "glm-5.3-flash",
        "kimi-k3-1",
        "minimax-m3"
      ],
      "transformer": {
        "use": ["openai"]
      }
    }
  ],
  "Router": {
    "default": "wb2api,deepseek-v4.1-flash",
    "background": "wb2api,glm-5.3-flash",
    "think": "wb2api,deepseek-v4-pro",
    "longContext": "wb2api,hy4-preview"
  }
}
```

要点：
- `api_base_url` 必须是**完整**的 `.../v1/chat/completions`（铁律二）。
- `transformer.use` 必须是 `["openai"]` —— 这就是让 CCR 知道下游是 OpenAI 格式的开关。
- `models` 里列出的名字，就是 Claude Code 侧能选用的模型。

**第二步：让 Claude Code 指向 CCR** —— `C:\Users\<你的用户名>\.claude\settings.json`

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "http://127.0.0.1:3456",
    "ANTHROPIC_AUTH_TOKEN": "wb2api-local",
    "ANTHROPIC_MODEL": "deepseek-v4.1-flash",
    "ANTHROPIC_SMALL_FAST_MODEL": "glm-5.3-flash"
  }
}
```

- `ANTHROPIC_BASE_URL` = **CCR 的地址**（3456 是 CCR 默认端口），不是网关地址。
- `ANTHROPIC_AUTH_TOKEN` 只是给本地 CCR 的占位值，**不是**网关的 `api_key` —— 真实 key 写在第一步的 CCR 配置里。
- 不要把这些写进**项目里**的 `.claude/settings.json`（会被提交到 git）。用用户级的 `~/.claude/settings.json`。

**第三步：启动**

```powershell
ccr code
```

注意命令是 **`ccr code`**，不是 `claude`。CCR 会拉起 Claude Code 并把自己插在中间。

**没装 Node / 想省事？** CCR 需要 Node。也可以用 LiteLLM 之类的本地代理做同样的翻译，原理一致。

---

### 4.2 Codex CLI（直连，最省事）

Codex 原生说 OpenAI 格式，`wire_api = "chat"` 就能直接吃本网关。

**配置** —— `%USERPROFILE%\.codex\config.toml`

```toml
model = "deepseek-v4.1-flash"
model_provider = "wb2api"

[model_providers.wb2api]
name = "WorkBuddy2API"
base_url = "http://127.0.0.1:7863/v1"
env_key = "WB2A_CODEX_KEY"
wire_api = "chat"
```

**设置密钥并启动：**

```powershell
$env:WB2A_CODEX_KEY = "test_key"
codex
```

要点：
- **`wire_api` 必须是 `"chat"`，不能是 `"responses"`** —— 本网关只有 `/v1/chat/completions`，没有 `/v1/responses`。
- `base_url` 写到 `/v1` 为止，不要把 `/chat/completions` 写进去。
- `model_provider` 的值要和 `[model_providers.xxx]` 的 `xxx` 完全一致。

---

### 4.3 Cline / Roo Code / Kilo Code（VS Code 插件）

在扩展设置里：

| 字段 | 值 |
|---|---|
| API Provider | **OpenAI Compatible** |
| Base URL | `http://127.0.0.1:7863/v1` |
| API Key | `test_key` |
| Model ID | `deepseek-v4.1-flash` |

Kilo Code 之类支持自动拉取模型列表的，点一下就行。

---

### 4.4 Continue.dev（VS Code / JetBrains）

**新版本**用 YAML —— `%USERPROFILE%\.continue\config.yaml`

```yaml
name: wb2api
version: 0.0.1
schema: v1
models:
  - name: WB DeepSeek V4.1 Flash
    provider: openai
    model: deepseek-v4.1-flash
    apiBase: http://127.0.0.1:7863/v1
    apiKey: test_key
    useResponsesApi: false
  - name: WB GLM 5.3 Flash
    provider: openai
    model: glm-5.3-flash
    apiBase: http://127.0.0.1:7863/v1
    apiKey: test_key
    useResponsesApi: false
```

**老版本**用 JSON —— `%USERPROFILE%\.continue\config.json`

```json
{
  "models": [
    {
      "title": "WB DeepSeek V4.1 Flash",
      "provider": "openai",
      "model": "deepseek-v4.1-flash",
      "apiBase": "http://127.0.0.1:7863/v1",
      "apiKey": "test_key"
    }
  ]
}
```

`useResponsesApi: false` 关键 —— 强制走 chat/completions。

---

### 4.5 Aider（命令行结对编程）

```powershell
$env:OPENAI_API_BASE = "http://127.0.0.1:7863/v1"
$env:OPENAI_API_KEY  = "test_key"
aider --model openai/deepseek-v4.1-flash
```

要点：模型名前缀 **`openai/` 必须加**，否则 Aider 不会用你自定义的 base URL。

永久生效（写进用户环境变量）：

```powershell
[System.Environment]::SetEnvironmentVariable('OPENAI_API_BASE', 'http://127.0.0.1:7863/v1', 'User')
[System.Environment]::SetEnvironmentVariable('OPENAI_API_KEY', 'test_key', 'User')
```

---

### 4.6 Cursor

1. `Settings` → `Models` → 展开 `OpenAI API Key`
2. 填入 key：`test_key`，并打开 **Override OpenAI Base URL** 开关
3. Base URL 填 `http://127.0.0.1:7863/v1`
4. 在 `Add model` 里手工加 `deepseek-v4.1-flash`，关闭不需要的内置模型

> Cursor 偏「补全 / 单轮对话」，长 agent 任务交给 Claude Code 或 Cline 体验更好。

---

### 4.7 Cherry Studio / ChatBox / Open WebUI（桌面 / 自托管 GUI）

套路完全一致：

1. 供应商类型选 **OpenAI**
2. API Host / Base URL 覆盖为 `http://127.0.0.1:7863/v1`（个别只填 host 的填 `http://127.0.0.1:7863`）
3. 填 key `test_key`
4. 点「获取模型列表」，或手工添加 第 3 节 里的模型 id

> 有些 GUI 会把推理模型的 `reasoning_content` 直接渲染在正文里——那是客户端渲染习惯，不是返回坏了。

---

### 4.8 代码里直接调（SDK / LangChain / Dify）

**OpenAI 官方 SDK（Python）**

```python
from openai import OpenAI

client = OpenAI(base_url="http://127.0.0.1:7863/v1", api_key="test_key")
resp = client.chat.completions.create(
    model="deepseek-v4.1-flash",
    messages=[{"role": "user", "content": "只回复 OK"}],
)
print(resp.choices[0].message.content)
```

**LangChain**

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="deepseek-v4.1-flash",
    base_url="http://127.0.0.1:7863/v1",
    api_key="test_key",
)
```

**Dify**：`设置` → `模型供应商` → `OpenAI-API-compatible` → 填 Base URL 与 key → 添加模型名。

---

### 4.9 其他 CLI（同一套路，按需取用）

| 工具 | 配置方式 |
|---|---|
| **Zed** | Settings → LLM Providers → 添加 OpenAI-compatible，API URL 填 `http://127.0.0.1:7863/v1` |
| **Qwen Code** | `$env:OPENAI_BASE_URL="http://127.0.0.1:7863/v1"`；`$env:OPENAI_API_KEY="test_key"`；`$env:OPENAI_MODEL="deepseek-v4.1-flash"` |
| **Goose** | `$env:GOOSE_PROVIDER="openai"`；`$env:OPENAI_HOST="http://127.0.0.1:7863"`（不带 /v1）；`$env:OPENAI_BASE_PATH="v1/chat/completions"` |
| **OpenHands** | LLM 设置里 Custom Model 填 `openai/deepseek-v4.1-flash`，Base URL 填 `http://127.0.0.1:7863/v1` |

---

## 5. 用 Claude Code / Codex 必看：`prompt.mode` 会顶掉它们的系统提示词

这是最容易踩、也最容易被误判成「模型变笨了」的坑。

### 现象

用 `custom` 模式（**默认值**）时，网关在出站前会**删除客户端全部的 `system` / `developer` 消息**，只留一条网关自己的 ~2KB 通用 system（`internal/prompt/prompt.go:68-87`，`README` §系统提示词体系）。

而 Claude Code / Codex 的系统提示词正是它们 **agent 行为的核心**（工具使用规范、TodoWrite 规则、"你是编码代理"的身份约束等）。被通用提示词顶掉后，它们的表现会明显退化：不肯用工具、跳步骤、不遵守项目约定。

### 正确姿势

用 Claude Code / Codex 这类**自带人格与工具协议**的 CLI 时，把提示词模式改成 `passthrough`（透传客户端原始 system）：

编辑上游仓库的 `config.json`：

```json
"prompt": {
  "mode": "passthrough",
  "file": ""
}
```

然后重启容器：

```powershell
cd <上游仓库目录>
docker compose restart wb2api
```

**不用怕指纹误报** —— 项目作者正是为这个场景做了指纹脱敏（`internal/upstream/sanitize.go:15-21`，特征表里全是 Claude Code / Codex 的模板句，含 `You are Claude Code`、`You are a coding agent running in the Codex CLI`、`Main branch (` 等）。`features.sanitize_blacklist_fingerprints` 默认就是 `true`，会把这些模板句做最小改写（每句只改一个词，语义不变）绕过逐字匹配，而**不动**你的业务指令。

另外 `passthrough` 模式还有自动降级：首次撞上内容策略误报，会在**同一次请求内**换中性提示词重试一遍（`README` §内容拦截误报与降级重试）。

### 什么时候保持 `custom`

纯 OpenAI 兼容的**非 agent** 用途（Cherry Studio 闲聊、脚本批量调用、自己写的 LLM 应用）——你没有自有 system 需要保全，用 `custom` 更省事，而且能顺带绕开 system 来源的误报。

| 客户端 | 建议 `prompt.mode` |
|---|---|
| Claude Code / Codex CLI / Cline / Roo / Continue（agent 型的） | `passthrough` |
| Cherry Studio / ChatBox / 脚本 / 自研应用 | `custom`（默认） |

---

## 6. 排错对照表

| 症状 | 多半原因 | 怎么办 |
|---|---|---|
| `404` / 提示找不到路径 | Claude Code 直连了网关 | 走 CCR（铁律一） |
| `404`，日志里路径是 `/v1/chat/completions/chat/completions` | base URL 多带了 `/chat/completions` | 改成 `http://127.0.0.1:7863/v1` |
| `404`，日志里是 `/chat/completions`（少了 /v1） | base URL 少写 `/v1` | 补上 |
| `model not found` / 模型不存在 | 用了旧的 `deepseek-v4-flash` | 换成 `deepseek-v4.1-flash` |
| `401 invalid_api_key` | key 与 `config.json` 的 `api_key` 不一致 | 对齐；或 `config.json` 里留空则完全不鉴权 |
| `503 no_healthy_account: all accounts unavailable` | 账号池全在冷却 / 被禁用 / 占满 | 面板看「账号池」表，或一键签到解冻 |
| 客户端一说话就断 / 无输出 | 客户端不支持 SSE，或代理吃掉了流 | 换 `stream:false`，或检查中间代理 |
| 模型「变笨」、不调工具、不守规矩 | `prompt.mode=custom` 顶掉了系统提示词 | 改 `passthrough`（第 5 节） |
| 工具调用不生效 | 客户端没用 OpenAI 格式的 tools | 确认客户端走的是 chat/completions 的 tools 字段 |
| 面板打不开 | 面板服务没跑 | 双击 `启动面板.bat` |
| `端口已被占用` | 已有一个面板在跑 | 直接用现有的 `http://127.0.0.1:7864`，或先关掉旧的 |

---

## 7. 安全提醒

- **`api_key` 还是示例值 `test_key`**。本机自用无妨；**要用到局域网/公网前必须换成强随机 key**，并考虑把 compose 的端口映射从 `0.0.0.0:7863` 收窄到 `127.0.0.1:7863`。
- `auths/*.json` 里是**明文 accessToken / refreshToken**，别提交 git、别外传。
- 网关**没有内置 TLS**，别直接暴露到公网，要暴露就前置反向代理。
- 上游是腾讯 CodeBuddy 的**非官方**接口。账号风险、条款风险由使用者自行承担，**仅限本人授权账号、私有环境自用**。

---

*相关文件：`启动面板.bat`（启动面板）、`server.py`（本地面板服务）、`index.html`（面板页面）、`README.md`（面板说明）。*
